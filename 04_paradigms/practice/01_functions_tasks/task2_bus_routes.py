"""
Задание 2: Оптимизация автобусных маршрутов

Транспортный отдел города тревожится: на одних маршрутах в час пик
не протолкнуться, на других — пара пассажиров на весь салон. Чтобы
сравнивать маршруты между собой, число пассажиров делят на длину
маршрута. Считается, что маршрут с загруженностью ниже 200 пассажиров
на километр нужно пересматривать — вероятно, по нему ходит слишком мало
автобусов.

Напишите две функции:

1. load_per_km(route) — загруженность маршрута как число пассажиров
   на один километр (passengers / length), округлённое до 1 знака.
   Вход: dict {"number": int, "length": float, "passengers": int}

2. needs_review(load) — возвращает True, если загруженность меньше 200,
   то есть маршрут требует пересмотра.
"""


def load_per_km(route):
    pass


def needs_review(load):
    pass