"""
Задание 4: Генератор зашумлённых измерений

Лаборатория измеряет температуру в теплице, но датчик «шумит»: показания
слегка плавают вокруг истинного значения. Чтобы получить стабильную
картину, каждый реальный замер повторяют трижды, а потом усредняют.
Датчик снимает тысячи значений в час — выдавать готовый список на все
замеры расточительно, поэтому измерения поступают по одному.

Функция noisy_measurements(base_values, noise_level=0.1) — генератор.
Для каждого значения из base_values выдаёт 3 зашумлённых измерения:

    value * (1 + random.uniform(-noise_level, noise_level))

Используйте yield для каждого измерения.
"""


def noisy_measurements(base_values, noise_level=0.1):
    yield