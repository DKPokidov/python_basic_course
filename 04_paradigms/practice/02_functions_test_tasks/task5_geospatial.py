"""
Задание 5: Геопространственный анализ

Функция find_least_served_districts(data, n=3) определяет районы с наименьшим
количеством социальных объектов. Площади районов в данных не указаны, поэтому
«плотность» вычисляется как количество объектов района (длина списка объектов
с данным значением поля "district").

Структура входных данных data:

    {"city": str, "year": int, "objects": [объекты]}

Каждый объект — словарь с полями "type", "name", "district", "capacity",
"coordinates", "accessibility", "services".

Возвращает список первых n кортежей (район, плотность), отсортированный
по возрастанию плотности (при равных значениях сохраняется порядок обхода районов).
"""


def find_least_served_districts(data, n=3):
    pass