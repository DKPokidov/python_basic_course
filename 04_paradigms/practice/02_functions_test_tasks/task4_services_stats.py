"""
Задание 4: Статистика по услугам

Функция services_stats(data) собирает все уникальные услуги по типам объектов.
Возвращает словарь {тип: [список_услуг]}.

Структура входных данных data:

    {"city": str, "year": int, "objects": [объекты]}

Каждый объект — словарь с полями "type", "name", "district", "capacity",
"coordinates", "accessibility", "services". Услуги объекта — список строк в поле
"services". Услуга учитывается один раз даже если встречается у нескольких объектов.
"""


def services_stats(data):
    pass